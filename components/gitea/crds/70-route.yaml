apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: gitea
spec:
  host: "{{ gitea.host }}"
  to:
    kind: Service
    name: gitea-http
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
