# Shared / agent
SSL_VERIFY={{ monitor.ssl_verify | string | lower }}
LOG_LEVEL={{ monitor.log_level }}
TRACE_DB_PATH={{ monitor.trace_db_path }}

# Chat & monitor (in-cluster agent service)
AGENT_URL=http://agent.{{ agent.namespace }}.svc:8080
AGENT_TIMEOUT={{ monitor.agent_timeout }}
