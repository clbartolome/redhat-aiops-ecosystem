apiVersion: apps/v1
kind: Deployment
metadata:
  name: itsm-app
  labels:
    app: itsm-app
spec:
  replicas: 1
  # Recreate: SQLite + ReadWriteOnce PVC cannot attach to two pods during a rollout.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: itsm-app
  template:
    metadata:
      labels:
        app: itsm-app
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: itsm-app
          image: "{{ itsm.image }}"
          imagePullPolicy: Always
          command:
            - /bin/sh
            - -c
            - >-
              python3 /opt/itsm-entrypoint/session_max_age.py &&
              exec uvicorn app.main:app --host 0.0.0.0 --port 8000
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - secretRef:
                name: itsm-config
          volumeMounts:
            - name: itsm-entrypoint
              mountPath: /opt/itsm-entrypoint
              readOnly: true
            - name: sqlite-data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      volumes:
        - name: itsm-entrypoint
          configMap:
            name: itsm-entrypoint
            defaultMode: 0555
        - name: sqlite-data
          persistentVolumeClaim:
            claimName: itsm-data
