# Shared / agent
SSL_VERIFY={{ chat.ssl_verify | string | lower }}
LOG_LEVEL={{ chat.log_level }}
TRACE_DB_PATH={{ chat.trace_db_path }}

# Chat & monitor (in-cluster agent service)
AGENT_URL=http://agent.{{ agent.namespace }}.svc:8080
AGENT_TIMEOUT={{ chat.agent_timeout }}
