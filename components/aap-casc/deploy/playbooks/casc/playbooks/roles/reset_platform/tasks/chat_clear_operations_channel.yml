---
- name: Note that chat 1.5 channel history is client-side only
  ansible.builtin.debug:
    msg: >-
      Chat 1.5 stores channel history in the browser (localStorage). There is no server API
      to clear the operations channel. Use the Clear channel button in the chat UI after reset,
      or open chat in a fresh browser session.
